analyzer:
  strong-mode:
    implicit-casts: false
  errors:
    todo: ignore
  exclude:
    - flutter/**
    - lib/api/*.dart
    - .dart_tool
    - example/gmail_xoauth2

linter:
  rules:
    - avoid_empty_else
    - cancel_subscriptions
    - close_sinks

include: package:lints/recommended.yaml